# yaml-language-server: $schema=https://raw.githubusercontent.com/ShadowBlip/InputPlumber/main/rootfs/usr/share/inputplumber/schema/capability_map_v2.json
version: 2
kind: CapabilityMap
name: AYN Odin Gamepad Mapping
id: ayn_odin

# The Odin's face buttons are labeled in the Nintendo arrangement (A east,
# B south, X north, Y west) and the odin-gamepad driver reports positional
# codes. Map label->function so pressing "A" confirms.
mapping:
  - name: Guide Button
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_MODE, value_type: button}
    target_event:
      gamepad: {button: Guide}

  - name: South Button (physical A, east)
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_EAST, value_type: button}
    target_event:
      gamepad: {button: South}

  - name: East Button (physical B, south)
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_SOUTH, value_type: button}
    target_event:
      gamepad: {button: East}

  - name: West Button (physical X, north)
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_NORTH, value_type: button}
    target_event:
      gamepad: {button: West}

  - name: North Button (physical Y, west)
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_WEST, value_type: button}
    target_event:
      gamepad: {button: North}

  - name: Start Button
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_START, value_type: button}
    target_event:
      gamepad: {button: Start}

  - name: Select Button
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_SELECT, value_type: button}
    target_event:
      gamepad: {button: Select}

  - name: Left Bumper
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_TL, value_type: button}
    target_event:
      gamepad: {button: LeftBumper}

  - name: Right Bumper
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_TR, value_type: button}
    target_event:
      gamepad: {button: RightBumper}

  - name: Left Stick Click
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_THUMBL, value_type: button}
    target_event:
      gamepad: {button: LeftStick}

  - name: Right Stick Click
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_THUMBR, value_type: button}
    target_event:
      gamepad: {button: RightStick}

  - name: Dpad Up
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_DPAD_UP, value_type: button}
    target_event:
      gamepad: {button: DPadUp}

  - name: Dpad Down
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_DPAD_DOWN, value_type: button}
    target_event:
      gamepad: {button: DPadDown}

  - name: Dpad Left
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_DPAD_LEFT, value_type: button}
    target_event:
      gamepad: {button: DPadLeft}

  - name: Dpad Right
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_DPAD_RIGHT, value_type: button}
    target_event:
      gamepad: {button: DPadRight}

  # Back buttons: M1 (left) -> L4, M2 (right) -> R4. The DT emits
  # TRIGGER_HAPPY7/5 to match inputplumber's native paddle convention.
  - name: Left Back Button (M1)
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_TRIGGER_HAPPY7, value_type: button}
    target_event:
      gamepad: {button: LeftPaddle1}

  - name: Right Back Button (M2)
    source_events:
      - evdev: {event_type: KEY, event_code: BTN_TRIGGER_HAPPY5, value_type: button}
    target_event:
      gamepad: {button: RightPaddle1}

  # Triggers are a Gamepad:Trigger capability, not an axis — declaring them
  # as axis:{name: LeftTrigger} resolved to no capability and inputplumber
  # silently dropped the mapping (dead triggers with a clean journal).
  - name: Left Trigger
    source_events:
      - evdev: {event_type: ABS, event_code: ABS_Z, value_type: trigger}
    target_event:
      gamepad:
        trigger:
          name: LeftTrigger

  - name: Right Trigger
    source_events:
      - evdev: {event_type: ABS, event_code: ABS_RZ, value_type: trigger}
    target_event:
      gamepad:
        trigger:
          name: RightTrigger

  - name: Left Stick
    source_events:
      - evdev: {event_type: ABS, event_code: ABS_X, value_type: joystick_x}
      - evdev: {event_type: ABS, event_code: ABS_Y, value_type: joystick_y}
    target_event:
      gamepad:
        axis:
          name: LeftStick

  - name: Right Stick
    source_events:
      - evdev: {event_type: ABS, event_code: ABS_RX, value_type: joystick_x}
      - evdev: {event_type: ABS, event_code: ABS_RY, value_type: joystick_y}
    target_event:
      gamepad:
        axis:
          name: RightStick
