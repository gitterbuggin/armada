# sdm845-odin bring-up: the LAN Forgejo registry serves plain HTTP.
# Remove (with the matching policy.json entry) before any public image.
[[registry]]
location = "forgejo.example.invalid"
insecure = true
